import React from 'react'

const Function = (props) => {
  return (
    <div>{props.text}</div>
  )
}

const Text1 = (props) => {
    return (
        <div>{props.text}</div>
    )
}

export default Function