import React, { Component } from "react";

export default class Home extends Component{
    render(){
        return (
            <div>This is class component.Now with out refresh it will update its content.Lets check for another time.</div>
        );
    }
}

